There are three types of actions in a workflow: <DocsLink href="#execute-actions">execute actions</DocsLink>, <DocsLink href="#for-each-actions">for-each actions</DocsLink>, and <DocsLink href="#include-actions">include actions</DocsLink>. They are differentiated by their `type` attribute.
