There are two types of actions in a workflow: <DocsLink href="#execute-actions">execute actions</DocsLink>
and <DocsLink href="#for-each-actions">for-each actions</DocsLink>.
They are differentiated by their `type` attribute.
