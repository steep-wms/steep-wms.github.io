import ScrollLink from "../../components/ScrollLink"

The configuration file `plugins/common.yaml` describes all plugins so Steep can
compile and use them during runtime. The file contains an array of objects
with the properties described in the section
on <ScrollLink href="#extending-steep-through-plugins">extending Steep through plugins</ScrollLink>.
