import Admonition from "@/components/Admonition"

In the following, we present two ways to extend the workflow from the <DocsLink href="#sequential-workflows">previous tutorial</DocsLink> to download and extract multiple files at the same time.

<Admonition>
This section assumes that you've read and followed the previous tutorial and that you've already configured the metadata for the download and extract services.
</Admonition>