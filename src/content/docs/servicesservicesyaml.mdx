import ScrollLink from "../../components/ScrollLink"

The file `services/services.yaml` contains an array of <ScrollLink href="#service-metadata">service metadata objects</ScrollLink>
describing the interfaces of all processing services Steep can execute.
