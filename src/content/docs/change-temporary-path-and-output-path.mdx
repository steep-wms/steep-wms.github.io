Open Steep's <DocsLink href="#steepyaml">main configuration file</DocsLink> `conf/steep.yaml` and modify the properties `tmpPath` and `outPath` as follows:

<CodeContainer title="conf/steep.yaml">
```yaml
steep:
  tmpPath: /data/tmp
  outPath: /data/out
```
</CodeContainer>
