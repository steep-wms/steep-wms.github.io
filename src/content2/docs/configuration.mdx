import DocsLink from "components/DocsLink"

After you have <DocsLink href="#download-and-get-started">downloaded and extracted</DocsLink>
Steep, you can find its configuration files under the `conf` directory. The
following sections describe each of these files in detail.
